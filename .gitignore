node_modules
dist
.release-state.json
.DS_Store
coverage/
.coverage-tmp/
test-results/
playwright-report/
.mcp.json
.claude/settings.local.json
/.hotsheet/*
!/.hotsheet/settings.json
/.glassbox/*
!/.glassbox/settings.json

# Demo capture artifacts (debug screenshots + scratch/probe scripts)
scripts/demo/.debug/
scripts/demo/_*

# HAR network logs captured alongside the demo screenshots — useful for
# debugging a capture run but not worth tracking (multi-MB binary churn).
*.har

# Tauri
src-tauri/target
src-tauri/binaries
src-tauri/server

# Ground-truth screenshot regression — regenerated actuals (baselines are LFS-committed)
ground-truth-screenshots/actuals/
ground-truth-screenshots/*.har

# External fixture repos used by the ground-truth screenshot suite (cloned, not vendored)
external/


# Built content-plugin bundles (doc 29); generated by scripts/build-plugins.mjs
plugins/*/index.js
