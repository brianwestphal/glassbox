{
  "$schema": "https://json.schemastore.org/sarif-2.1.0.json",
  "version": "2.1.0",
  "runs": [
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mt28agmt5rz76k0q",
          "message": {
            "text": "All three close paths now funnel through one tracked dismiss so the autoReposition listeners can't leak. Previously the picker closed three ways that each just detached the element: reopen (dismissExistingPopups → .remove()), an option click (popup.remove()), and outside-click (dismissOnOutsideClick). Only the last ran cleanup. Now positionBelowAnchor returns a scroll/resize disposer (see popup.ts), so a bare .remove() would strand those listeners — the module-level activeDismiss holds the single open picker's full teardown, reopen calls it, and the option-click path calls dismiss() instead of popup.remove(). Same pattern in riskPopover.tsx.\n",
            "markdown": "All three close paths now funnel through one tracked `dismiss` so the autoReposition listeners can't leak. Previously the picker closed three ways that each just detached the element: reopen (`dismissExistingPopups` → `.remove()`), an option click (`popup.remove()`), and outside-click (`dismissOnOutsideClick`). Only the last ran cleanup. Now `positionBelowAnchor` returns a scroll/resize disposer (see `popup.ts`), so a bare `.remove()` would strand those listeners — the module-level `activeDismiss` holds the single open picker's full teardown, reopen calls it, and the option-click path calls `dismiss()` instead of `popup.remove()`. Same pattern in `riskPopover.tsx`.\n"
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/client/annotations/reclassifyPopup.tsx"
                },
                "region": {
                  "startLine": 31,
                  "endLine": 66,
                  "snippet": {
                    "text": "let activeDismiss: (() => void) | null = null;\n\nfunction dismissExistingPopups(): void {\n  activeDismiss?.();\n  activeDismiss = null;\n  // Safety net for any untracked stray popup (defensive; the tracked dismiss\n  // above handles the normal case).\n  document.querySelectorAll('.reclassify-popup').forEach(el => { el.remove(); });\n}\n\n/** Generic category picker: opens the shared popup anchored to `anchor`,\n *  highlighting `current`, and invokes `onPick` with the chosen category value.\n *  The two specialized entry points below build on this, as does image feedback\n *  (doc 23). */\nexport function showCategoryPicker(\n  anchor: HTMLElement,\n  current: string,\n  onPick: (value: string) => void,\n): void {\n  dismissExistingPopups();\n  const popup = renderPopup(current);\n  const stopReposition = positionBelowAnchor(popup, anchor);\n  document.body.appendChild(popup);\n\n  const dismiss = dismissOnOutsideClick(popup, () => {\n    stopReposition();\n    activeDismiss = null;\n  });\n  activeDismiss = dismiss;\n\n  void delegate(popup, 'click', '.reclassify-option', (e, opt) => {\n    e.stopPropagation();\n    const value = asEl(opt).dataset.value ?? '';\n    // Route through dismiss (not a bare popup.remove()) so the reposition\n    // listeners are cleaned up along with the element + outside-click listener.\n    dismiss();"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ]
          },
          "workItemUris": [
            "GB-1133"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "408a875344d5cdd55372375862ef68c2"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/glassbox.git",
          "revisionId": "90f263df90f71526f736f3efed0ec7d7340eb4aa",
          "branch": "main"
        }
      ]
    }
  ]
}
